<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="imagePagerTheme" parent="@android:style/Theme.DeviceDefault">
        <!--这里放空不填<item name="android:windowAnimationStyle">@style/ActivityFade</item>-->
        <item name="android:windowAnimationStyle">@style/ActivityFade</item>
    </style>

    <style name="ActivityFade" parent="@android:style/Animation.Activity" >
        <item name="android:activityOpenEnterAnimation">@anim/activity_fade_enter</item>
        <item name="android:activityOpenExitAnimation">@anim/activity_fade_exit</item>
        <item name="android:activityCloseEnterAnimation">@anim/activity_fade_enter</item>
        <item name="android:activityCloseExitAnimation">@anim/activity_fade_exit</item>
    </style>

</resources>
